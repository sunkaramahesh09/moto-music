<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Only used for the launch window; every pixel after that is drawn by Compose.
      A matching -night variant keeps the cold-start frame from flashing white.
    -->
    <style name="Theme.MotoMusic.Starting" parent="@android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/launch_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
</resources>
